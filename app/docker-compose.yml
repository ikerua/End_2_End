# ⚠️  ESTE ARCHIVO YA NO ES EL PRINCIPAL
#
# El docker-compose principal está en la RAÍZ del proyecto:
#   End_2_End/docker-compose.yml
#
# Ejecuta todos los comandos desde la raíz:
#   cd End_2_End/
#   docker compose up whisper-cpu-slim
#   docker compose up whisper-gpu-slim
#   docker compose up whisper-cpu-bundled
#   docker compose up whisper-gpu-bundled
